import { styled } from "@mui/material/styles";

export const StyledCell = styled("div")({
  display: "flex"
});

export const StyledCellHeader = styled("div")({});

export const StyledCellBody = styled("div")({});
